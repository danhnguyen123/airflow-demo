#Copy docker-install.sh into Start script VM

#Check output start-script
sudo journalctl -u google-startup-scripts.service

#Start service
git clone 
cd airflow-demo
docker-compose up -d --build
